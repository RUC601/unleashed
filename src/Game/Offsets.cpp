#include "Game/Offsets.hpp"

// Offsets are compile-time constants defined in Offsets.hpp.
// This file exists as a translation unit to anchor any future
// runtime-reloadable offset tables if needed.
//
// All offsets are static constexpr and require no initialization.
// Game version updates may require updating the values in the header.
